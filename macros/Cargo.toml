[package]
name = "cellarium-macros"
version = "0.1.0"
edition = "2021"
description = "Proc macros for cellarium: #[derive(CellState)] and #[cell]"
license = "MIT OR Apache-2.0"

[lib]
proc-macro = true

[dependencies]
syn = { version = "2", features = ["full"] }
quote = "1"
proc-macro2 = "1"
